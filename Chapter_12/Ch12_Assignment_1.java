package Programs.Chapter_12;

public class Ch12_Assignment_1
{
    public static void main(String []args)
    {
        // Q. What is the Value of x^x for any value of x ?

        // --> if we think about it, the XOR operation with X ^ X  will always return 0. Because XOR returns 0 when
        // the comparison is with same digits so x^X XOR = 0
    }
}